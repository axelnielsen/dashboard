<p id="notice"><%= notice %></p>

<h1>Registration Detail2s</h1>

<table>
  <thead>
    <tr>
      <th>Registration head2</th>
      <th>Competition</th>
      <th>Bestachievement</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @registration_detail2s.each do |registration_detail2| %>
      <tr>
        <td><%= registration_detail2.registration_head2 %></td>
        <td><%= registration_detail2.competition %></td>
        <td><%= registration_detail2.bestAchievement %></td>
        <td><%= link_to 'Show', registration_detail2 %></td>
        <td><%= link_to 'Edit', edit_registration_detail2_path(registration_detail2) %></td>
        <td><%= link_to 'Destroy', registration_detail2, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Registration Detail2', new_registration_detail2_path %>
