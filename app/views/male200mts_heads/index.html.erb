<p id="notice"><%= notice %></p>

<h1>Male200mts Heads</h1>

<table>
  <thead>
    <tr>
      <th>Championship</th>
      <th>Fecha</th>
      <th>Hora</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @male200mts_heads.each do |male200mts_head| %>
      <tr>
        <td><%= male200mts_head.championship_id %></td>
        <td><%= male200mts_head.fecha %></td>
        <td><%= male200mts_head.hora %></td>
        <td><%= link_to 'Show', male200mts_head %></td>
        <td><%= link_to 'Edit', edit_male200mts_head_path(male200mts_head) %></td>
        <td><%= link_to 'Destroy', male200mts_head, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Male200mts Head', new_male200mts_head_path %>
