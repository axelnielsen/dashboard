<p id="notice"><%= notice %></p>

<h1>Start Female200mts</h1>

<table>
  <thead>
    <tr>
      <th>Place</th>
      <th>Achievement</th>
      <th>Rail</th>
      <th>Number</th>
      <th>Athlete</th>
      <th>An</th>
      <th>Club</th>
      <th>Region</th>
      <th>Country</th>
      <th>Female200mtshead</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @start_female200mts.each do |start_female200mt| %>
      <tr>
        <td><%= start_female200mt.place %></td>
        <td><%= start_female200mt.achievement %></td>
        <td><%= start_female200mt.rail %></td>
        <td><%= start_female200mt.number %></td>
        <td><%= start_female200mt.athlete %></td>
        <td><%= start_female200mt.an %></td>
        <td><%= start_female200mt.club %></td>
        <td><%= start_female200mt.region %></td>
        <td><%= start_female200mt.country %></td>
        <td><%= start_female200mt.female200mtsHead_id %></td>
        <td><%= link_to 'Show', start_female200mt %></td>
        <td><%= link_to 'Edit', edit_start_female200mt_path(start_female200mt) %></td>
        <td><%= link_to 'Destroy', start_female200mt, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Start Female200mt', new_start_female200mt_path %>
